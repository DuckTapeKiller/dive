name: Build desktop releases

on:
  push:
    tags:
      - "v*"
      - "[0-9]*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macOS
            os: macos-latest
            command: npx electron-builder --mac dmg --x64 --arm64 --publish never
            files: |
              release/*.dmg
              release/*.dmg.blockmap
          - platform: Windows
            os: windows-latest
            command: npx electron-builder --win nsis portable --x64 --publish never
            files: |
              release/*.exe
              release/*.exe.blockmap
              release/*.yml
          - platform: Linux
            os: ubuntu-latest
            command: npx electron-builder --linux AppImage deb --x64 --publish never
            files: |
              release/*.AppImage
              release/*.AppImage.blockmap
              release/*.deb

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm

      - name: Install Linux packaging dependencies
        if: matrix.platform == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libarchive-tools

      - run: npm ci
      - run: ${{ matrix.command }}

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v2.2.2
        with:
          files: ${{ matrix.files }}
